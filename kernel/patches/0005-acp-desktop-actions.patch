diff --git a/crates/codegen/xai-grok-shell/src/agent/mvp_agent/acp_agent.rs b/crates/codegen/xai-grok-shell/src/agent/mvp_agent/acp_agent.rs
index 1027c440..e67b0a13 100644
--- a/crates/codegen/xai-grok-shell/src/agent/mvp_agent/acp_agent.rs
+++ b/crates/codegen/xai-grok-shell/src/agent/mvp_agent/acp_agent.rs
@@ -2262,6 +2262,16 @@ impl acp::Agent for MvpAgent {
                 crate::extensions::feedback::handle(self, &args).await
             }
             "x.ai/recap" => crate::extensions::recap::handle(self, &args).await,
+            "x.ai/desktop/workflow/launch" => {
+                crate::extensions::desktop_actions::launch(self, &args).await
+            }
+            "x.ai/desktop/workflow/manage" => {
+                crate::extensions::desktop_actions::manage(self, &args).await
+            }
+            "x.ai/desktop/goal" => crate::extensions::desktop_actions::goal(self, &args).await,
+            "x.ai/desktop/command" => {
+                crate::extensions::desktop_actions::command(self, &args).await
+            }
             "x.ai/cloud/terminate" => {
                 crate::extensions::auth_gate::require_xai_auth(
                     &self.auth_manager,
diff --git a/crates/codegen/xai-grok-shell/src/extensions/desktop_actions.rs b/crates/codegen/xai-grok-shell/src/extensions/desktop_actions.rs
new file mode 100644
index 00000000..18064c40
--- /dev/null
+++ b/crates/codegen/xai-grok-shell/src/extensions/desktop_actions.rs
@@ -0,0 +1,176 @@
+//! Structured actions used by desktop clients.
+//!
+//! These routes are thin adapters over the existing session actor. They do not
+//! implement a second workflow engine or agent loop; kernel-owned managers and
+//! prompt handling remain authoritative.
+use super::{ExtResult, parse_params, to_raw_response};
+use crate::agent::mvp_agent::MvpAgent;
+use crate::session::SessionCommand;
+use crate::session::plan_mode::PromptMode;
+use agent_client_protocol as acp;
+use serde::Deserialize;
+use serde_json::json;
+use tokio::sync::oneshot;
+
+#[derive(Debug, Deserialize)]
+struct WorkflowLaunchRequest {
+    #[serde(rename = "sessionId")]
+    session_id: String,
+    name: String,
+    #[serde(default)]
+    input: String,
+}
+
+#[derive(Debug, Deserialize)]
+struct WorkflowManageRequest {
+    #[serde(rename = "sessionId")]
+    session_id: String,
+    #[serde(rename = "runId")]
+    run_id: String,
+    op: String,
+}
+
+#[derive(Debug, Deserialize)]
+struct GoalRequest {
+    #[serde(rename = "sessionId")]
+    session_id: String,
+    objective: String,
+}
+
+#[derive(Debug, Deserialize)]
+struct DesktopCommandRequest {
+    #[serde(rename = "sessionId")]
+    session_id: String,
+    command: String,
+    #[serde(default)]
+    arguments: String,
+}
+
+async fn session_handle(
+    agent: &MvpAgent,
+    session_id: &str,
+) -> Result<crate::session::SessionHandle, acp::Error> {
+    if session_id.trim().is_empty() {
+        return Err(acp::Error::invalid_params().data("missing sessionId"));
+    }
+    agent
+        .session_handle_waiting_for_load(&acp::SessionId::new(session_id.trim()))
+        .await
+        .ok_or_else(|| acp::Error::invalid_params().data("unknown session id"))
+}
+
+/// Launch a saved workflow through the session's real WorkflowManager.
+pub async fn launch(agent: &MvpAgent, args: &acp::ExtRequest) -> ExtResult {
+    let request: WorkflowLaunchRequest = parse_params(args)?;
+    let handle = session_handle(agent, &request.session_id).await?;
+    let (respond_to, response) = oneshot::channel();
+    handle
+        .cmd_tx
+        .send(SessionCommand::DesktopWorkflowLaunch {
+            name: request.name.trim().to_string(),
+            input: request.input,
+            respond_to,
+        })
+        .map_err(|_| acp::Error::internal_error().data("session actor is unavailable"))?;
+    let message = response.await.map_err(|_| {
+        acp::Error::internal_error().data("session actor dropped workflow response")
+    })?;
+    to_raw_response(&json!({ "message": message }))
+}
+
+/// Manage a workflow run through the same actor path used by slash commands.
+pub async fn manage(agent: &MvpAgent, args: &acp::ExtRequest) -> ExtResult {
+    let request: WorkflowManageRequest = parse_params(args)?;
+    let handle = session_handle(agent, &request.session_id).await?;
+    let (respond_to, response) = oneshot::channel();
+    handle
+        .cmd_tx
+        .send(SessionCommand::DesktopWorkflowManage {
+            run_id: request.run_id.trim().to_string(),
+            op: request.op.trim().to_string(),
+            respond_to,
+        })
+        .map_err(|_| acp::Error::internal_error().data("session actor is unavailable"))?;
+    let message = response.await.map_err(|_| {
+        acp::Error::internal_error().data("session actor dropped workflow response")
+    })?;
+    to_raw_response(&json!({ "message": message }))
+}
+
+/// Start a Goal through the existing prompt/goal orchestrator.
+pub async fn goal(agent: &MvpAgent, args: &acp::ExtRequest) -> ExtResult {
+    let request: GoalRequest = parse_params(args)?;
+    let objective = request.objective.trim();
+    if objective.is_empty() {
+        return Err(acp::Error::invalid_params().data("missing objective"));
+    }
+    let handle = session_handle(agent, &request.session_id).await?;
+    let outcome = send_desktop_prompt(&handle, &format!("/goal {objective}")).await?;
+    match outcome {
+        Ok(result) => to_raw_response(&json!({
+            "stopReason": format!("{:?}", result.stop_reason),
+        })),
+        Err(error) => Err(error),
+    }
+}
+
+/// Run a kernel slash command from a desktop control. The command is validated
+/// here, then parsed and gated by the existing session command machinery.
+pub async fn command(agent: &MvpAgent, args: &acp::ExtRequest) -> ExtResult {
+    let request: DesktopCommandRequest = parse_params(args)?;
+    let command = request.command.trim().trim_start_matches('/');
+    if command.is_empty()
+        || command.len() > 128
+        || !command
+            .chars()
+            .all(|ch| ch.is_ascii_alphanumeric() || matches!(ch, '-' | '_' | '.'))
+    {
+        return Err(acp::Error::invalid_params().data("invalid desktop command"));
+    }
+    let handle = session_handle(agent, &request.session_id).await?;
+    let prompt = if request.arguments.trim().is_empty() {
+        format!("/{command}")
+    } else {
+        format!("/{command} {}", request.arguments.trim())
+    };
+    let outcome = send_desktop_prompt(&handle, &prompt).await?;
+    match outcome {
+        Ok(result) => to_raw_response(&json!({
+            "stopReason": format!("{:?}", result.stop_reason),
+        })),
+        Err(error) => Err(error),
+    }
+}
+
+async fn send_desktop_prompt(
+    handle: &crate::session::SessionHandle,
+    prompt: &str,
+) -> Result<crate::session::PromptTurnResult, acp::Error> {
+    let (respond_to, response) = oneshot::channel();
+    let prompt_id = format!("desktop-action-{}", uuid::Uuid::now_v7());
+    handle
+        .cmd_tx
+        .send(SessionCommand::Prompt {
+            prompt_id,
+            prompt_blocks: vec![acp::ContentBlock::Text(acp::TextContent::new(
+                prompt.to_string(),
+            ))],
+            prompt_mode: PromptMode::Agent,
+            artifact_upload_ctx: None,
+            client_identifier: Some("grok-desktop".to_string()),
+            screen_mode: None,
+            verbatim: false,
+            traceparent: None,
+            json_schema: None,
+            send_now: false,
+            admission: None,
+            tool_overrides_update: None,
+            respond_to,
+            persist_ack: None,
+            parsed_prompt_tx: None,
+        })
+        .map_err(|_| acp::Error::internal_error().data("session actor is unavailable"))?;
+    response
+        .await
+        .map_err(|_| acp::Error::internal_error().data("session actor dropped desktop response"))
+}
diff --git a/crates/codegen/xai-grok-shell/src/extensions/mod.rs b/crates/codegen/xai-grok-shell/src/extensions/mod.rs
index 55703911..bcea579a 100644
--- a/crates/codegen/xai-grok-shell/src/extensions/mod.rs
+++ b/crates/codegen/xai-grok-shell/src/extensions/mod.rs
@@ -6,6 +6,7 @@ pub(crate) mod chat_conversation_history;
 pub mod code_nav;
 pub mod consent;
 pub mod debug;
+pub mod desktop_actions;
 pub mod feedback;
 pub mod fs;
 pub mod git;
diff --git a/crates/codegen/xai-grok-shell/src/session/acp_session_impl/run_loop.rs b/crates/codegen/xai-grok-shell/src/session/acp_session_impl/run_loop.rs
index 31926e02..b82e50a1 100644
--- a/crates/codegen/xai-grok-shell/src/session/acp_session_impl/run_loop.rs
+++ b/crates/codegen/xai-grok-shell/src/session/acp_session_impl/run_loop.rs
@@ -1846,6 +1846,25 @@ pub(super) async fn run_session(
                                 s.handle_recap(auto).await;
                             });
                         }
+                        SessionCommand::DesktopWorkflowLaunch {
+                            name,
+                            input,
+                            respond_to,
+                        } => {
+                            let (registry, _) = session.named_workflow_snapshot();
+                            let message = session
+                                .launch_named_workflow(&registry, &name, &input)
+                                .await;
+                            let _ = respond_to.send(message);
+                        }
+                        SessionCommand::DesktopWorkflowManage {
+                            run_id,
+                            op,
+                            respond_to,
+                        } => {
+                            let message = session.manage_workflow_run(&run_id, &op).await;
+                            let _ = respond_to.send(message);
+                        }
                         SessionCommand::AISuggest { prefix, cwd, model_override, respond_to } => {
                             let s = session.clone();
                             tokio::task::spawn_local(async move {
diff --git a/crates/codegen/xai-grok-shell/src/session/commands.rs b/crates/codegen/xai-grok-shell/src/session/commands.rs
index 30e83252..44b57621 100644
--- a/crates/codegen/xai-grok-shell/src/session/commands.rs
+++ b/crates/codegen/xai-grok-shell/src/session/commands.rs
@@ -813,6 +813,19 @@ pub enum SessionCommand {
         /// `false` for an explicit `/recap`.
         auto: bool,
     },
+    /// Launch a named workflow from a structured desktop action. The existing
+    /// workflow manager remains the only execution path.
+    DesktopWorkflowLaunch {
+        name: String,
+        input: String,
+        respond_to: oneshot::Sender<String>,
+    },
+    /// Manage a kernel-owned workflow run from a structured desktop action.
+    DesktopWorkflowManage {
+        run_id: String,
+        op: String,
+        respond_to: oneshot::Sender<String>,
+    },
     /// Request an AI-generated shell command suggestion.
     ///
     /// The session actor builds a minimal prompt from `prefix` + `cwd`,
