# 0.4 假 / 半真功能处理计划

原则：**不做假成品；半真要有真链路或诚实标注。**

## 已在 0.4.0 落地

| 项 | 做法 |
|----|------|
| Hermes 记忆 | 默认开；分层文件；开局注入；自动学习沉淀；记一条落盘 |
| 侧栏记忆 | 改为「记忆文件」管理入口，不是空壳开关 |
| FEATURES | 真 / 半 / 规划 诚实表 |

## 半真（有接线，继续加深）

| 项 | 后续 |
|----|------|
| Plan mode | 执行/重试/空计划提示已加；引擎 plan 质量仍依赖内核 |
| Worktree | 用作项目 / 新任务 / Finder / 返回主仓 / 侧栏 WT 标记 |
| Voice | 原生 STT 或 Whisper 后备 |
| Review | 复制 diff/路径、显示文件、非 git 预览 |
| Goal | 壳侧完整：持久化 + 控制台 + plan/工具进度 |
| + 菜单 slash | 首页过滤用 builtins 缓存；会话内对齐 available_commands |
| 多模型 | 连接测试 + 默认 + 选择器 + setModel 粘性；路由仍依赖 config |
| 侧栏任务筛选 | 已加标题过滤 |

## 当前边界（设置页，不假装可用）

| 设置项 | 当前状态 | 诚实边界 |
|---|---|---|
| 外观 | 已完成 | 主题和密度即时本地持久化。 |
| 浏览器 | 已完成 | 可连接并诊断隔离的 Playwright MCP；实际浏览动作仍由内核 MCP 运行时和 Chrome 决定。 |
| Git | 已完成 | 跳转到真实 Review，支持状态、差异、暂存与取消暂存。 |
| 桌面操控 | 半真 | 用户主动截取屏幕区域并附加到输入框是真实的；不提供鼠标/键盘自动化或后台截屏。 |
| Hooks | 已接线 | 当前会话可加载内核发现的 Hooks，并显式重载、信任/取消信任、启用/停用；不在 App 中伪造 Hook 作者或执行器。 |
