# gorkX feature matrix

See **`docs/MASTER_PLAN.md`** and **`docs/INDEPENDENT_APP_PLAN.md`**.

**Rule:** no fake product surfaces. Real = works end-to-end. Half = wired to engine/local with limits. Planned = not shipped.

**Upstream parity rule:** every useful Grok Build capability is reviewed at each
locked-kernel update. A capability that ACP exposes is presented in gorkX as a
clear desktop flow; a TUI-only capability is implemented through a maintained
kernel ACP extension, or is explicitly marked unavailable until that extension
exists. gorkX never relabels a browser API, a typed slash command, or a local
mock as the corresponding Grok Build capability.

**Connector UX rule:** ordinary users connect third-party platforms through a clear browser/system authorization flow and return to gorkX. Manual tokens, URLs, CLIs and config files are advanced fallbacks only; a platform without a real authorization chain is shown as unavailable/Soon, never as connected.

**Desktop-first UX rule:** the primary path for every user-facing capability is a named button, guided dialog, picker, card, or workspace panel. Slash commands remain available as an expert keyboard shortcut and for direct Grok Build compatibility, but are never the only documented or discoverable way to use a desktop feature. This is essential for office, research, operations, writing, and other non-programming work.

| Area | Feature | Status |
|------|---------|--------|
| Layout | Codex three-pane + collapsible sidebar | **Real** |
| Layout | Review / Terminal / Process | **Real** |
| Kernel | ACP stdio, App `GROK_HOME`, bundled engine path, Doctor | **Real** — normal installs do not scan `~/.grok`, `~/.agents`, external compatibility files or PATH for kernel configuration; Grok Build child processes receive an App-owned `HOME` and `GROK_HOME`. Doctor reports missing bundle, auth and data-folder access |
| Environment | Managed Grok Build configuration preview and install | **Kernel-wired, confirmation-gated** — Settings → Environment first fetches `grok setup --json`. Only a successful current preview enables “Install previewed configuration”; that action requires a second explicit confirmation and invokes only exact `grok setup`, never arbitrary flags. It cannot silently alter a running task and tells the user to reconnect tasks afterward. Availability still requires the authenticated kernel service. |
| Chat | Stream, tools, permissions, ACP resource-link attachments, / @ keyboard nav | **Real** — a task explicitly started in Full permission mode also advertises bounded ACP client text-file writes. Writes remain confined to the selected project, reject symlinks and are atomically replaced; Default and Auto tasks do not advertise this bypass path. Switching project/home clears the unsent draft, staged attachments and armed workflow so material from one project cannot be accidentally sent in another. |
| Conversation results | Markdown headings, lists, code, blockquotes, **step cards**, read-only completion checklists, **GFM pipe tables**, automatic charts for compact two-column numeric comparisons, safe data-only `chart` blocks, common `mermaid` flowcharts, and colored `diff` / `patch` code blocks | **Real** — ordinary user turns carry a small presentation preference: comparisons and structured work should use tables when useful, and genuine 2–6-way decisions may return clickable choices. The agent still chooses whether it applies; code and ordinary prose are not forced into charts. Visual results render inside the conversation automatically; tables retain their exact values. Chart blocks accept only bounded numeric bar/line data, Mermaid support is a bounded local flowchart reader (TD/LR nodes and arrows), and diff rendering only colors escaped unified-diff text. No HTML, scripts or remote assets execute. |
| Task deliverables | **本次成果 / Deliverables** button appears only when the active task has explicitly returned a file or media attachment; it opens the existing local preview / Finder flow | **Real, deliberately conservative** — it aggregates agent-returned attachments in the task and never scans a project directory or labels modified files as deliverables by guesswork. Bundled Grok Build ACP image output is a verified producer. Standard ACP `resource_link` file output is accepted only for a real, regular, non-symlink absolute file inside the current project, after native validation; remote links, missing files, external paths and oversize files never become local attachments. |
| Guided answers | Explicit fenced `choices` / `options` blocks become clickable quick replies | **Real** — only a user click sends the selected value to Grok Build; ordinary lists remain non-interactive |
| Suggested follow-ups | Grok Build's ACP `x.ai/follow_ups` notifications render as bounded clickable next-question chips below the current task | **Kernel-wired** — gorkX accepts only a non-empty bounded response id and up to six sanitized labels from the originating task. A chip is never auto-sent, disappears when the next main turn begins, and does not turn ordinary assistant lists into actions. It appears only when the engine actually emits the notification. |
| Decision inbox | Live Grok Build permission, structured-question, plan-review and project-trust requests stay in one cross-task **待处理 / Decisions** queue; the current task still shows a structured decision card inline | **Kernel-wired** — every row is a real outstanding ACP request keyed to its originating task and JSON-RPC request. A response is removed only after the original connected task accepts it; disconnected tasks are removed rather than shown as actionable. The queue is intentionally live-only, not a fabricated deferred-job system. |
| Chat | Fork conversation | **Kernel-wired** — 0.2.110 stdio exposes the compatibility route `_x.ai/session/fork`; gorkX creates and loads the durable child session while preserving the source. The standard `x.ai/*` spelling is not accepted by this stdio runtime, so the client uses the tested runtime route. |
| Chat | Rewind conversation | **Kernel-wired** — gorkX lists `_x.ai/rewind/points`, requires an explicit checkpoint and scope, then treats `force: false` as the kernel's non-mutating preview. The already confirmed restore action commits with `force: true`; conflicts are shown and require a second explicit acknowledgement before any overwrite. Package `0.2.112 (47348d1)` has passed a real two-turn preview, commit and reload acceptance run. |
| Chat | Follow up during a running task | **Real, local queue** — the desktop keeps a typed follow-up and sends it as the next normal turn after the current task completes. The 0.2.112 stdio ACP rejects both `x.ai/btw` and `_x.ai/btw` with `Method not found`, so gorkX deliberately ships no false "Ask aside" button or answer card. The expert `/btw` compatibility command remains recognizable but clearly reports this runtime limitation. |
| Chat | Task info | **Kernel-wired** — the `+` menu opens a read-only **Task info / 任务信息** panel with the live model, agent, turns, working folder, tool calls, context-capacity meter, and the safe session authentication category (OAuth / API Key / external / not signed in). The maintained ACP adapter returns no credential material and offers the matching Account or Models & providers settings destination. It calls the verified compatibility route `_x.ai/session/info`; no model prompt is sent. |
| Task approach | Existing **Plan / 规划模式** and new-task **Explore project / 探索项目** controls | **Kernel-wired** — a new Plan task supplies a bounded portable `agentProfile` in ACP `session/new`, then enters the engine's native Plan mode. Explore passes Grok Build's tested built-in `explore` profile, whose own toolset and Plan permission mode make it a read-only project-mapping task. Both controls affect only a newly created task; gorkX does not replace the kernel planner, recreate its restrictions in the shell, or silently switch an existing task. |
| Session diagnostics | `+ → Export diagnostic archive` saves a local Grok Build trace after a privacy notice; separate `+ → Send diagnostics to Grok support` sends the active task's trace only after an immediate, explicit warning and confirmation | **Real, confirmation-gated** — local export invokes only `grok trace <session> --local --output <user-picked .tar.gz>`. The separate support path invokes only `grok trace <session> --json`; it never uploads automatically, never chains an export into an upload, and never exposes arbitrary flags. Both paths warn that task/tool diagnostics may be included. |
| Image / video generation and image editing | **Generate image** / **Generate video** buttons open natural-language forms. When an image is staged, **Edit attached image** prepares an explicit edit request without removing that attachment; supported ACP raster output appears in the conversation as a local image card. Settings may write Grok Build's optional `image_edit_model_override`. | **Engine-gated** — supported by the locked source, but the active Grok account and engine configuration decide whether a request can run. Image editing is an engine tool, not a guessed `/imagine-edit` command: gorkX sends the user-confirmed intent plus image and the kernel selects its available tool. The override chooses a model only if the account actually exposes it. When the engine emits a PNG/JPEG/GIF/WebP ACP image block, gorkX validates it, saves bytes only under App data (not SQLite/logs/transcript text), persists path metadata for task restore, and renders it locally. Slash commands remain expert compatibility only. |
| Goal | **Set goal** form, persisted banner, status/pause/resume/clear → agent; progress from plan / `update_goal` tool | **Real (shell)** — engine goal loop quality still varies; slash syntax is expert compatibility only. |
| Projects / Tasks | Create, archive, delete, SQLite index | **Real** |
| Local task search | Sidebar **Search all tasks** button (or `⌘⇧F`) searches gorkX-owned task titles and persisted conversation snapshots across projects, including archived tasks; selecting a result restores its local snapshot and project context (and intentionally restores an archived task to the active list) | **Real** — entirely local SQLite search with bounded excerpts/results; it does not scan project files, a user CLI home, attachments, or send a model request |
| Scheduled | SQLite jobs; foreground tasks plus opt-in macOS launchd worker; Grok Build session loops | **Real with limits** — gorkX background runs every 5 minutes in Grok plan mode only (no silent repository writes), save output locally, require the installed macOS app; each worker claim/recovery/completion uses an SQLite immediate transaction, and an unreported claim is retried only after a 30-minute lease with recorded backoff. Local arrangements can be edited in place: their ID and execution history remain intact while the next run is recalculated from the new rule. Separately, when the live kernel advertises `/loop`, `+ → Repeat with Grok` opens a plain-language cadence form and delegates the short-lived (seven-day) recurring task to Grok Build's own `scheduler_create` path. Kernel-created tasks render a live conversation card from `ScheduledTaskCreated/Fired/Deleted` events and can be cancelled through the documented `x.ai/scheduler/delete` route; they are intentionally not represented as gorkX-managed jobs. |
| Native subagents | Lifecycle events → persisted parent/child task tree; completed children can be inspected and active children can be stopped directly from that tree; reconnect reconciles engine-reported running children; Settings policy for enabling all, `explore`, and `plan` types | **Kernel-wired** — gorkX uses the maintained `list_running/get/cancel` runtime routes for the task tree. The current control plane does not provide a desktop creation/assignment route, so the kernel chooses delegation, capability mode and worktree isolation. |
| Background workflows | Grok Build `workflow_updated` ACP events render as live cards in the originating conversation, with phases, active agents, budget consumption, elapsed time, pause reason and result summary; eligible cards offer Pause / Resume buttons. Saved workflows advertised by the active kernel session appear as Run buttons in the `+` menu, with an optional-context confirmation form. | **Kernel-wired** — gorkX only projects bounded kernel updates and keeps a readable summary when a task is restored. A saved workflow is shown only when ACP supplies its `workflowSource` metadata, and is rechecked against the live task before launch. Pause / Resume appears only when that live task advertises the kernel `workflow` command and its server-provided handle passes a strict safe-name check. It does not create a second workflow scheduler or invent management controls for unavailable routes. |
| Terminal | PTY dock | **Real** |
| Extensions | Skills / MCP / plugins via engine CLI and App `GROK_HOME` | **Real** — a Skill’s Run button opens a natural-language task form; it does not require a user to compose its slash syntax. Installed plugins can show the kernel's component inventory and validate their selected local folder. The plugin market can add a Git URL, GitHub shorthand or local source, refresh a source or all sources, and remove a source with a clear warning that its installed plugins are removed too. MCP includes native remote and local setup: remote uses a name, HTTPS endpoint and HTTP/SSE transport; local uses the system file picker for an executable and one argument per line, never shell parsing. Both choose all projects or the current project. A provider's OAuth remains its official browser flow after task connection. The desktop refresh button re-reads the live task inventory; a configuration change that needs process reload is not represented as a hidden hot-refresh API. Remote setup rejects embedded credentials and headers; both local and remote setup require an explicit confirmation before persistent configuration. Depends on engine and does not read `~/.grok` by default. |
| Memory (Hermes) | Default on; USER/AGENT/project files; inject on first prompt; auto-learn dumps; Remember / Forget / keyword search / local compact | **Real (v0.4+)** — kernel `/flush`/`/dream` still optional extras. gorkX-only auto-learn is stored separately under App Support, never as an unrecognized Grok Build `config.toml` key. |
| Personal instructions | Settings → Personalization → Personal instructions | **Real** — gorkX writes only its fixed `$GROK_HOME/rules/gorkx-personal.md` file, which Grok Build natively scans for every newly started Agent. The desktop never asks users to compose `--rules`; a saved change applies to new or reconnected tasks and can be cleared from the same screen. |
| Auth / quota | Browser device login; stay signed in; silent refresh; membership + avatar | **Real (v0.4.1+)** — gorkX displays a percentage only when Grok Build's live billing response actually supplies it. The current CLI response can omit the Grok web subscription meter; in that case gorkX never saves or labels a manually entered percentage as current usage, and instead offers a one-click route to the official signed-in Usage page for the authoritative value. |
| Daily token record | Local daily total plus input/output breakdown in **Settings → Usage & billing** | **Real (v0.4.4+)** — gorkX records only the per-turn `PromptResponse.usage` actually returned by Grok Build ACP and de-duplicates that turn by its message ID. Session context snapshots are displayed only as context and never accumulated. It does not label this session-side record as provider quota, cost, or an estimate. |
| App update | Settings check + download DMG; launch banner | **Real (v0.4.2)** — drag into Applications to replace |
| Onboarding | First-run checklist: engine · login · project | **Real (v0.4.3)** |
| Start screen | Button-first office and project workflows: read & summarize, draft a report, meeting minutes, email draft, research & compare, decision brief, plan a project, explore code, fix a bug, add a feature, write tests | **Real** — each card pre-fills the localized, editable composer brief. The user can tailor it or add files, then explicitly sends it to create the normal durable Grok Build task; no slash command or second agent loop is involved. |
| Plan mode | setMode + review steps + execute / retry after fail; native `x.ai/exit_plan_mode` approval gate with plan preview, revision feedback, approval and abandon outcomes | **Kernel-wired** — response shape matches bundled Grok Build 0.2.112 source; it appears only when the engine actually requests plan approval, while plan quality still varies by engine/model |
| Worktree | create / list / use / new task / Finder / **back to main repo** | **Real** — isolated ACP regression creates a kernel worktree in a disposable Git project and confirms its returned path; actual repository conflict handling remains an explicit user confirmation path |
| Voice Mode / dictation | Grok Build streaming STT and macOS low-memory capture helper | **Kernel-wired** — gorkX adds a maintained `x.ai/voice/*` ACP adapter around Grok Build's own CoreAudio capture and streaming STT pipeline; it does not use browser Web Speech. The microphone button appears only in a live task, streams interim text locally below the composer, inserts only a finalized transcript into the editable draft, and never auto-sends it. The native ACP probe checks start/stop/shutdown routing against a guaranteed-missing session, so no test capture, TCC prompt, audio upload, or model request occurs. A manual macOS microphone-permission and spoken-transcript acceptance run remains a release gate. |
| Review Diff | Git porcelain + file diff; copy path/diff; reveal file; non-git **file preview** | **Real with a stated boundary** — non-git folders receive a safe file preview, not a fabricated unified diff. The preview is available only for a user-selected project folder; inbox tasks never scan their home-directory fallback. |
| GitHub | Settings → Git offers browser Device Flow first: it opens GitHub’s official approval page, shows a copyable one-time code, polls only the temporary in-memory request, then stores the returned authorization in macOS Keychain. Review → Remote reads the current origin's open PRs; checks and discussion/review comments load on demand. Fine-grained tokens remain an advanced fallback. | **Real, confirmation-gated** — gorkX owns a registered OAuth App and requests only `read:user` + `public_repo` through the browser flow, so public-repository Review/PR work needs no pasted token. Private repositories, organisation policies, and rate-limit recovery can use a user-created fine-grained token instead. Review can create a PR only after the user supplies title/base/body, explicitly confirms, and has already pushed the branch; it never pushes a branch or edits local files. Posting a PR discussion comment is likewise a separate explicit confirmation. |
| Custom API / compatible models | Settings quick setup for OpenAI API / Anthropic API / OpenRouter API / Google Gemini API / local Ollama / compatible gateway; provider model-directory reader; App `config.toml`; task/session selection and default; provider labels/groups; macOS Keychain | **Real** — quick setup pre-fills only real protocol endpoints and opens the provider's official key/download page; it never claims web subscriptions are API connections. A user can read a configured provider's standard model directory and select a returned ID rather than guess one; this call never saves the form, logs a key, or exposes provider response bodies. The released bundled engine was isolated-tested to advertise and accept a configured `[model.*]` through ACP `session/set_model`; after a settings change, active kernel processes receive native `reload_models` so the model need not wait for a task restart. The connection probe is one small provider request and reports success only after it verifies generated text in the matching protocol response. Saved models visibly retain a non-secret pass/fail state and timestamp, so “configured” is not presented as “verified”. Endpoint response bodies never reach the UI or logs. |
| Multiple provider subscriptions | Account aggregation, OAuth and quota across ChatGPT/Claude/Grok web subscriptions | **Soon** — Grok login is real; OpenAI/Anthropic currently require a user API key or compatible gateway, and web subscriptions are never presented as API logins |
| Sidebar | Task title filter across projects / inbox | **Real** |
| + menu and `/` completion | `+` provides native forms and panels for normal work; `/` remains keyboard compatibility and expert completion | **Real** — task info, compact, **recap**, export, **share**, skills, memory and extensions use desktop controls. The Recap button appears only when the live kernel advertises `/recap` and requests the kernel-generated conversation summary without exposing command syntax. Share appears only when `/share` is advertised, uses Grok Build's `x.ai/share_session` route, and requires an immediate privacy confirmation before creating/copying a link. When a live session advertises Grok Build's `deep-research` workflow, `+ → Deep research` opens a plain-language research brief form; it runs the kernel workflow and its progress stays in the conversation. When the kernel advertises `feedback`, `+ → Send feedback` opens its own form and routes the message through that session. Any newly advertised, non-sensitive engine action that lacks a dedicated control appears under **More engine workflows** as a description-led card and opens a plain-language form; it is still gated to the exact live ACP catalogue and does not expose slash syntax in the transcript. |
| Reply actions | Every rendered assistant reply has a local **Copy** button | **Real** — copies the already-rendered reply to the system clipboard; it makes the useful `/copy` outcome available per reply without a model call, an engine command, or exporting unrelated conversation content. |
| Settings · Appearance | System / light / dark theme plus compact / comfortable / spacious density; instant local persistence | **Real** |
| Browser reading | The common **Add → Open web source** action (and Browser settings) opens an explicitly supplied HTTP(S) source in a separate gorkX reading window; Browser settings also connect / diagnose a version-pinned, isolated Playwright MCP for Chrome with an optional origin allowlist, and can disable Grok Build’s built-in web search/fetch for new or reconnected tasks | **Real** — new connections use `@playwright/mcp@0.0.78`, an in-memory profile and blocked service workers. The web-research setting launches the kernel with its documented root `--disable-web-search` flag and is locally persisted; it does not retroactively alter a running task. The user reading window validates HTTP(S), has no gorkX credential bridge, does not use browser sign-in state or send a model request, and is separate from the Agent's Chrome MCP profile; browser tools still depend on Chrome and the kernel MCP runtime. |
| Settings · Git | Opens real project Review (status, diff, stage / unstage) | **Real** |
| Settings · Computer | Explicit macOS screen-region picker → local PNG attached to the composer; **Computer Hub workspace** status / expose / pause / resume / disconnect controls | **Kernel-wired, server-gated** — capture remains local and user initiated. Computer Hub uses Grok Build's hidden `workspace` command with an App-selected project only; its status is read-only, while every connection-changing action requires a clear local confirmation that project content may reach the remote Hub. The server/account remains the final availability gate. gorkX does not claim to control the user's screen, mouse or keyboard directly. |
| Settings · Hooks & project instructions | Read / create / edit the project-root `AGENTS.md`; inspect, reload, trust/untrust, enable/disable, and add/remove a selected Hook JSON file or folder for the active task's Grok Build Hooks | **Kernel-wired** — all Hook actions use the verified `_x.ai/hooks/list` and `_x.ai/hooks/action` ACP routes from the live task. Adding uses a native file/folder picker; Grok Build validates the selected path belongs to the App `GROK_HOME` before loading it. gorkX never executes a Hook itself, never offers an arbitrary command box, and hides command / URL values that may contain sensitive data. Project Hooks remain inactive until the user explicitly trusts that project. |
| Settings · Agent sandbox | Built-in Grok Build `workspace`, `read-only`, `strict`, `devbox`, or `off` profile selector | **Real** — gorkX writes only the documented App `GROK_HOME/config.toml` `[sandbox].profile` field. Every newly started/reconnected ACP process begins in the selected task's canonical project directory before the kernel applies its OS sandbox; running tasks keep their existing boundary. Custom profile authoring remains an advanced, engine-owned config capability. |
| Project trust | Native `x.ai/folder_trust/request` prompt before project-local MCP, Hooks or LSP configuration is activated | **Kernel-wired** — gorkX advertises the interactive safety capability and returns only explicit `trust` or fail-closed `reject`; it appears only when the engine enables and requests the folder-trust gate |

## Deliberate limits still not shipped

- **Browser permissions:** an optional origin allowlist limits new Playwright MCP configurations; redirects remain an engine/MCP limitation and must still be visible to the user.
- **Computer automation:** only user-triggered local screen capture exists; no background capture or mouse/keyboard control.
- **Hook authoring:** gorkX can select an existing Hook JSON file or folder for Grok Build to load, but does not provide a free-form Hook authoring editor. Hook files remain project- or plugin-owned configuration; commands and URLs are intentionally not echoed in the desktop UI.
- **Provider subscriptions:** Grok login is real. OpenAI/Anthropic are API-key or compatible-gateway configurations; a ChatGPT/Claude web subscription is not treated as an API login.
- **Background schedules:** available only through the user-enabled macOS launchd worker; it is deliberately plan-only and does not create an interactive task while the app is closed.
- **Subagent delegation policy:** gorkX can persist and reconcile engine-reported child work, but it does not invent assignments, worktree isolation or completion results; those remain kernel-owned decisions.

## Run

```bash
cd apps/desktop && npm install && npm run tauri dev
```

## Isolated browser action gate

This sends no model prompt, but opens the explicit public origin in an
in-memory Playwright Chrome context. Use only an origin you intend to visit:

```bash
node scripts/verify-playwright-mcp.mjs --origin https://example.com
```

## Isolated custom-model ACP gate

This verifies that the bundled engine parses a disposable `[model.*]` entry,
advertises it to ACP and accepts `session/set_model`. It sends no model prompt,
but needs a copied cached Grok login in a disposable home:

```bash
GORKX_ACP_TEST_HOME=/private/tmp/gorkx-acp-home \
GORKX_ACP_TEST_CWD=/private/tmp/gorkx-acp-project \
node scripts/verify-grok-acp.mjs apps/desktop/src-tauri/resources/grok \
  --authenticated --custom-model
```

## Isolated task-authentication snapshot gate

This creates a disposable live session in an explicitly supplied isolated
authenticated App-owned home, and verifies that task information returns only
a bounded authentication category plus a safe settings destination. It sends
no model prompt and cannot expose a token:

```bash
GORKX_ACP_TEST_HOME=/private/tmp/gorkx-acp-home \
GORKX_ACP_TEST_CWD=/private/tmp/gorkx-acp-project \
node scripts/verify-grok-acp.mjs apps/desktop/src-tauri/resources/grok \
  --authenticated --session-info
```

## macOS bundle gate

After a local app-only build (not a release), validate its embedded engine without
using a system Grok installation:

```bash
scripts/verify-macos-app-bundle.sh "apps/desktop/src-tauri/target/release/bundle/macos/gorkX.app"
```

## Kernel diagnostics

Settings → Environment → Run doctor combines gorkX's app-owned engine/data-home
preflight with Grok Build's real `doctor --json` findings. An “Apply repair”
button appears only when the running kernel itself advertises an automatic
remediation; gorkX verifies that repair ID again before invoking
`grok doctor fix <id>`. It never exposes an arbitrary CLI command field.
